name: Deploy site-worker-mobile

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm install
      - run: npm run build

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/key
          chmod 600 ~/.ssh/key
          ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts
          ssh-keyscan -H 3.34.165.192 >> ~/.ssh/known_hosts

      - name: Deploy to dev (workerm.work365.com)
        run: |
          rsync -avz --delete \
            -e "ssh -i ~/.ssh/key" \
            dist/ ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:${{ secrets.EC2_TARGET_DIR }}/

      - name: Deploy to UAT (worker.work365.com)
        run: |
          rsync -avz --delete \
            -e "ssh -i ~/.ssh/key" \
            dist/ ubuntu@3.34.165.192:/var/www/site-worker-mobile/

      - name: Reload Nginx (dev + UAT)
        run: |
          ssh -i ~/.ssh/key ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} "sudo systemctl reload nginx || true"
          ssh -i ~/.ssh/key ubuntu@3.34.165.192 "sudo systemctl reload nginx"
